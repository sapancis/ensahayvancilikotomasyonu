@model ciftlik2.Models.AsiKaydi

@{
    ViewData["Title"] = "Aşı Kaydını Sil";
}

<div class="page-container">
    <div class="page-header">
        <h1><i class="fa-solid fa-trash"></i> Aşı Kaydını Sil</h1>
    </div>

    <div class="delete-container">
        <h3>Bu aşı kaydını silmek istediğinizden emin misiniz?</h3>
        <p>Bu işlem geri alınamaz.</p>

        <div class="record-details">
            <dl>
                <dt>Hayvan:</dt>
                <dd>@Model.Hayvan?.KupeNumarasi - @Model.Hayvan?.Ad</dd>

                <dt>Aşı Adı:</dt>
                <dd>@Model.AsiIsmi</dd>

                <dt>Aşı Tarihi:</dt>
                <dd>@Model.AsiTarihi?.ToString("dd.MM.yyyy")</dd>
            </dl>
        </div>

        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />

            <div class="delete-actions">
                <input type="submit" value="Evet, Sil" class="btn btn-delete" />
                <a asp-action="Index" class="btn btn-outline">İptal</a>
            </div>
        </form>
    </div>
</div>