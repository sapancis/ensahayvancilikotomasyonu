@model ciftlik2.Models.TestKaydi

@{
    ViewData["Title"] = "Test Kaydını Sil";
}

<div class="page-container">
    <div class="page-header">
        <h1><i class="fa-solid fa-trash"></i> Test Kaydını Sil</h1>
    </div>

    <div class="delete-container">
        <h3>Bu test kaydını silmek istediğinizden emin misiniz?</h3>
        <p>Bu işlem geri alınamaz.</p>

        <div class="record-details">
            <dl>
                <dt>Hayvan:</dt>
                <dd>@Model.Hayvan?.KupeNumarasi - @Model.Hayvan?.Ad</dd>

                <dt>Test Adı:</dt>
                <dd>@Model.TestIsmi</dd>

                <dt>Test Tarihi:</dt>
                <dd>@Model.TestTarihi?.ToString("dd.MM.yyyy")</dd>

                <dt>Sonuç:</dt>
                <dd>@Model.Sonuc</dd>
            </dl>
        </div>

        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />

            <div class="delete-actions">
                <input type="submit" value="Evet, Sil" class="btn btn-delete" />
                <a asp-action="Index" class="btn btn-outline">İptal</a>
            </div>
        </form>
    </div>
</div>